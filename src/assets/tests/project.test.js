const Project = require('../scripts/project');

describe('Project', () => {
    describe('function', () => {
        it('returns true', () => {
            expect(() => { return true}).toBe(true);
        });
    });
});
